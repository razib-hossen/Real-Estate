<odoo>
    <data>

        <!-- Action for Form View -->
        <record id="action_estate_property_type_form" model="ir.actions.act_window">
            <field name="name">Estate Property Type Form View</field>
            <field name="res_model">estate.property.type</field>
            <field name="view_mode">form</field>
        </record>

        <!-- Menu for the Estate Property -->
        <menuitem id="menu_estate_property_type" name="Settings" sequence="10" parent="menu_estate"/>

        <!-- Menu items linking to the actions -->
        <menuitem id="menu_estate_property_type_form" name="Property Types" parent="menu_estate_property_type" action="action_estate_property_type_form"/>

        <!-- Form View -->
        <record id="view_form_property_type" model="ir.ui.view">
            <field name="name">estate.property.type.form</field>
            <field name="model">estate.property.type</field>
            <field name="arch" type="xml">
                <form string="Test">
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                            </group>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

    </data>
</odoo>
